---
phase: 13-model-catalog
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - internal/provider/copilot/copilot.go
  - internal/provider/copilot/catalog_test.go
autonomous: true
requirements: [COPILOT-08, COPILOT-10]

must_haves:
  truths:
    - "Ping() uses fetchCatalog — succeeds when catalog returns ≥1 model"
    - "Ping() returns auth-specific error on 401 (token invalid/expired)"
    - "Ping() returns connectivity error on network failure"
    - "/model REPL command lists copilot models grouped under 'copilot'"
    - "All tests pass, project builds"
  artifacts:
    - path: "internal/provider/copilot/copilot.go"
      provides: "Ping() backed by catalog"
      contains: "fetchCatalog"
  key_links:
    - from: "internal/provider/copilot/copilot.go Ping()"
      to: "internal/provider/copilot/catalog.go fetchCatalog"
      via: "p.fetchCatalog(ctx)"
      pattern: "fetchCatalog"
---

<objective>
Replace the stub Ping() with a catalog-backed implementation, and verify the /model REPL command
correctly groups GitHub Models under the "copilot" provider heading.

Output: updated copilot.go (Ping via catalog), ping tests added to catalog_test.go
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PHASE-13-PLAN-SUMMARY.md
@.planning/phases/13-model-catalog/13-01-SUMMARY.md
@internal/provider/copilot/copilot.go
@internal/provider/copilot/catalog.go
@internal/provider/copilot/catalog_test.go
@internal/repl/repl.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace stub Ping() with catalog-backed implementation</name>
  <files>internal/provider/copilot/copilot.go</files>
  <read_first>
    - internal/provider/copilot/copilot.go (current Ping implementation)
    - internal/provider/copilot/catalog.go (fetchCatalog error handling)
  </read_first>
  <action>
Replace the current `Ping()` in copilot.go (which does a direct HTTP call) with a catalog-backed version:

```go
// Ping verifies the provider is reachable and authenticated by fetching the model catalog.
func (p *Provider) Ping(ctx context.Context) error {
    models, err := p.fetchCatalog(ctx)
    if err != nil {
        return fmt.Errorf("cannot connect to GitHub Models: %w", err)
    }
    if len(models) == 0 {
        return fmt.Errorf("copilot: GitHub Models catalog returned no models")
    }
    return nil
}
```

Also remove the now-unused `net/http` import and `catalogURL` constant from copilot.go if they are no longer needed there (fetchCatalog in catalog.go handles the HTTP call and uses catalogURL directly).

Check: does copilot.go still use `net/http` or `catalogURL` directly? If not, remove them to avoid unused import errors.
  </action>
  <verify>
    <automated>cd /Users/marcin.radoszewski/dev/fenec && go build ./internal/provider/copilot/... 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'fetchCatalog' internal/provider/copilot/copilot.go` matches in Ping()
    - `go build ./internal/provider/copilot/...` exits 0
  </acceptance_criteria>
  <done>Ping() uses fetchCatalog, project compiles</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Ping tests to catalog_test.go</name>
  <files>internal/provider/copilot/catalog_test.go</files>
  <read_first>
    - internal/provider/copilot/catalog_test.go (existing test patterns from plan 13-01)
    - internal/provider/copilot/copilot.go (new Ping implementation)
  </read_first>
  <action>
Add Ping test functions to `catalog_test.go`:

1. `TestPingSuccess` — mock returns valid catalog with ≥1 model. `Ping()` returns nil.

2. `TestPingAuthError` — mock returns 401. `Ping()` returns error containing "invalid or expired".

3. `TestPingNetworkError` — close the test server before calling Ping. Assert error is non-nil.

4. `TestPingEmptyCatalog` — mock returns `{"data":[]}`. Assert error is non-nil containing "no models".
  </action>
  <verify>
    <automated>cd /Users/marcin.radoszewski/dev/fenec && go test ./internal/provider/copilot/... -run TestPing -v 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'TestPing' internal/provider/copilot/catalog_test.go` returns 4 matches
    - `go test ./internal/provider/copilot/... -run TestPing -v` all pass
  </acceptance_criteria>
  <done>4 Ping tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Verify /model REPL grouping</name>
  <files></files>
  <read_first>
    - internal/repl/repl.go (handleModelCommand, providerModels listing logic)
  </read_first>
  <action>
Read the `/model` listing implementation in repl.go. Verify that:
1. It calls `ListModels()` on each registered provider
2. It groups results under the provider's `Name()` ("copilot")
3. It formats entries as `copilot/openai/gpt-4o-mini` etc.

If the grouping already works correctly with the copilot provider (it likely does since `Name()="copilot"` and `ListModels()` now returns real IDs), no code changes are needed — just document it in the summary.

If any display adjustment is needed (e.g., model IDs show double-prefix like "copilot/openai/gpt-4o-mini" vs just "openai/gpt-4o-mini"), fix it here.
  </action>
  <verify>
    <automated>cd /Users/marcin.radoszewski/dev/fenec && grep -n 'providerName\|modelID\|Name()\|ListModels\|FormatProvider' internal/repl/repl.go | head -20</automated>
  </verify>
  <acceptance_criteria>
    - REPL /model code confirmed to group by provider Name() and list each model ID
    - If changes needed: `go build ./...` exits 0 and test coverage maintained
    - Document finding in summary (no change needed OR change made + why)
  </acceptance_criteria>
  <done>REPL /model grouping works correctly with copilot provider</done>
</task>

<task type="auto">
  <name>Task 4: Full build and test suite verification</name>
  <action>
```bash
cd /Users/marcin.radoszewski/dev/fenec
go build ./...
go test ./internal/provider/copilot/... -v
go test ./internal/config/... -v
go vet ./...
```
All must pass.
  </action>
  <verify>
    <automated>cd /Users/marcin.radoszewski/dev/fenec && go build ./... && go test ./internal/provider/copilot/... && go vet ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0
    - `go test ./internal/provider/copilot/...` exits 0
    - `go vet ./...` exits 0
  </acceptance_criteria>
  <done>Full project builds and all tests pass</done>
</task>

</tasks>

<verification>
```bash
go build ./...
go test ./internal/provider/copilot/... -v
grep 'fetchCatalog' internal/provider/copilot/copilot.go
```
</verification>

<success_criteria>
1. Ping() delegates to fetchCatalog — no separate HTTP call in copilot.go
2. Ping fails with auth-specific message on 401
3. /model REPL grouping works: models listed under "copilot" heading
4. All tests pass (including 4 new Ping tests)
5. `go build ./...` and `go vet ./...` clean
</success_criteria>

<output>
Create `.planning/phases/13-model-catalog/13-02-SUMMARY.md` after completion.
</output>
