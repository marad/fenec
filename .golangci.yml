version: "2"

run:
  timeout: 3m

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - gosimple
    - unused
    - ineffassign

formatters:
  enable:
    - gofmt

linters-settings:
  lll:
    line-length: 120
